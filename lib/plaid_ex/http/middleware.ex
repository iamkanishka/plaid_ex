defmodule PlaidEx.HTTP.Middleware do
  @moduledoc """
  Namespace for PlaidEx HTTP middleware modules.

  Middleware is applied in the HTTP client pipeline. Each middleware
  transforms requests or handles responses.
  """
end
